faction_manifest_a_new_world_order_gru = {
	name = faction_manifest_a_new_world_order_gru_name
	description = faction_manifest_a_new_world_order_gru_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_countries
		completed_amount_collection = collection:reactionary_countries

		scale = {
			army_org_factor = 0.15
			army_attack_factor = 0.15
			production_factory_max_efficiency_factor = 0.10
			conscription_factor = 0.10
		}

		progress_sections = {
			manifest_fulfilled = {
				min = 0.75

				modifier = {
					resistance_target = -0.10
					drift_defence_factor = 0.10
					political_power_factor = 0.10
				}
			}
		}
	}
}


faction_goal_GRU_start_rearmament = {
	name = goal_GRU_start_rearmament_name
	description = goal_GRU_start_rearmament_desc
	category = short_term
	group = FOCUS_FILTER_INTERNAL_AFFAIRS
	
	allowed = { original_tag = GRU }
	
	completed = {
		num_of_military_factories > 45	
	}
	complete_effect = {
		add_faction_power_projection = 50
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_GRU_restore_eastern_borders = {
	name = goal_GRU_restore_eastern_borders_name
	description = goal_GRU_eastern_eastern_borders_desc
	category = medium_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { original_tag = GRU }
	
	completed = {
		1091 = { is_owned_by_ROOT_or_ally = yes }
		96 = { is_owned_by_ROOT_or_ally = yes }
		206 = { is_owned_by_ROOT_or_ally = yes }
		202 = { is_owned_by_ROOT_or_ally = yes }
		10 = { is_owned_by_ROOT_or_ally = yes }
	}
	complete_effect = {
		add_faction_power_projection = 150
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_GRU_defeat_internationale = {
	name = goal_GRU_defeat_internationale_name
	description = goal_GRU_defeat_internationale_desc
	category = long_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { original_tag = GRU }
	
	completed = {
		collection:european_countries = { has_socialist_government = no }
	}
	complete_effect = {
		add_faction_power_projection = 400
		add_faction_initiative = 1
		custom_effect_tooltip = tooltip_white_line
		every_faction_member = {
			limit = { 
				is_subject = no
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = { base = 100 }
}
faction_goal_GRU_defeat_russia = {
	name = goal_GRU_defeat_russia_name
	description = goal_GRU_defeat_russia_desc
	category = long_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { always = no }
	
	completed = {
		OR = {
			219 = { is_owned_by_ROOT_or_ally = yes }
			NOT = { country_exists = RUS }
			RUS = { has_capitulated = yes }
		}
	}
	complete_effect = {
		add_faction_power_projection = 350
		add_faction_initiative = 1
		custom_effect_tooltip = tooltip_white_line
		every_faction_member = {
			limit = { 
				is_subject = no
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = { base = 100 }
}
